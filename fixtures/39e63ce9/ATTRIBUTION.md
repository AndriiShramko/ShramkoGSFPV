# Fixture: SuperSplat scene 39e63ce9 (Modlinek Villa room)

Voxel collision (`scene.voxel.json`, `scene.voxel.bin`) and viewer settings (`settings.json`) of
the public SuperSplat scene https://superspl.at/scene/39e63ce9, stored exactly as served by the
SuperSplat CDN (the `.bin` is gzip-encoded, as S3 stores it). Used by unit tests and CI.

Scan by Andrii Shramko, licensed CC BY 4.0. Collision data generated by SuperSplat.
